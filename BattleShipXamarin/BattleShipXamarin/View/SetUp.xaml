<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BattleShipXamarin.View.SetUp">
    <ContentPage.Content>
        <StackLayout>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="1*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <Entry Text="Host"
                   IsReadOnly="True"
                   Grid.Row="1"
                   Grid.Column="1" />

                <Entry Text="Opponent"
                   IsReadOnly="True"
                   Grid.Row="1"
                   Grid.Column="2" />

                <Button 
                    Text="Ready"
                    Grid.Row="1"
                    Grid.Column="3" />
            </Grid>


            <Button x:Name="btnCreateRoom" Text="Create a new room" HorizontalOptions="Center"  
                      VerticalOptions="CenterAndExpand" Clicked="OnbtnCreateRoomClicked"> </Button>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>